import React from 'react';
import { Order } from './Models';

const useKanban = (terminal: string, orders: Order[]) : [kanbanColums: React.ReactElement[]] => {
  return [[]];
};

export default useKanban;